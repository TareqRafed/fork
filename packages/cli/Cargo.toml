[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
fork-core = { path = "../core", package = "core" }
boards = { path = "../boards" }
clap = { version = "4.5", features = ["derive"] }
dialoguer = "0.11"
anyhow = "1.0"
colored = "2.1"
